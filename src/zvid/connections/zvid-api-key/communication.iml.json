{
    "url": "https://api.zvid.io/api/user/profile",
    "method": "GET",
    "headers": {
        "x-api-key": "{{parameters.apiKey}}"
    },
    "response": {
        "valid": "{{statusCode === 200}}",
        "error": {
            "message": "[{{statusCode}}] {{ifempty(body.message, body.error)}}"
        },
        "metadata": {
            "type": "email",
            "value": "{{body.user.email}}"
        }
    },
    "log": {
        "sanitize": ["request.headers.x-api-key"]
    }
}
