[
    {
        "name": "target",
        "type": "select",
        "label": "Schema target",
        "required": true,
        "default": "project",
        "options": [
            {
                "label": "Project payload",
                "value": "project"
            },
            {
                "label": "Full render request",
                "value": "render-request"
            }
        ],
        "help": "Returns the caller-plan-aware JSON Schema, validation notes, professional authoring guidelines, and required authoring workflow."
    }
]
