{
    "url": "/api/jobs/{{parameters.jobId}}",
    "method": "GET",
    "response": {
        "output": {
            "jobId": "{{body.id}}",
            "state": "{{body.state}}",
            "url": "{{ifempty(body.result.url, body.result)}}",
            "thumbnailUrl": "{{body.result.thumbnailUrl}}",
            "failedReason": "{{body.failedReason}}",
            "raw": "{{body}}"
        }
    }
}
