{
    "url": "/api/templates",
    "method": "GET",
    "qs": {
        "limit": "{{parameters.limit}}"
    },
    "response": {
        "iterate": "{{body.templates}}",
        "output": "{{item}}",
        "limit": "{{parameters.limit}}"
    }
}
