{
    "url": "/{{parameters.url}}",
    "method": "{{parameters.method}}",
    "qs": {
        "{{...}}": "{{toCollection(parameters.qs, 'key', 'value')}}"
    },
    "headers": {
        "{{...}}": "{{toCollection(parameters.headers, 'key', 'value')}}"
    },
    "body": "{{parameters.body}}",
    "response": {
        "error": {
            "404": {
                "message": "[404] The requested Zvid API route was not found."
            }
        },
        "output": {
            "statusCode": "{{statusCode}}",
            "headers": "{{headers}}",
            "body": "{{body}}"
        }
    }
}
