{
    "condition": "{{if(webhook.secret, headers.`x-zvid-signature` = 'sha256=' + sha256(headers.`x-zvid-timestamp` + '.' + json(body), 'hex', webhook.secret), true)}}",
    "respond": {
        "status": 200
    },
    "output": "{{body}}"
}
