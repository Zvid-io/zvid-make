{
    "condition": "{{headers.`x-zvid-signature` = 'sha256=' + sha256(headers.`x-zvid-timestamp` + '.' + createJSON(body), 'hex', data.secret)}}",
    "respond": {
        "status": 200
    },
    "output": {
        "event": "{{body.event}}",
        "jobId": "{{body.jobId}}",
        "timestamp": "{{body.timestamp}}",
        "test": "{{body.test}}",
        "data": "{{body.data}}"
    }
}
